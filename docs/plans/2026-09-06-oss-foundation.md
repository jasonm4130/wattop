# OSS foundation

Prepare wattop for public development at `jasonm4130/wattop`, retaining MIT.
Success means a working remote, verified builds and tests, contributor and
security guidance, protected main, and a release pipeline without extra secrets.

Keep the existing dashboard changes. Add pinned CI actions, macOS race checks,
portable tests, CodeQL, Dependabot, issue forms, and contribution guidance.
Include the vendored collector's full license in source and release archives.
Document source installation until binary distribution is published and tested.

Verify workflow syntax, release configuration, native snapshot packaging, CLI
execution, secret scans, and GitHub checks. Enable repository protections after
the initial push so their required check names match observed CI runs.
